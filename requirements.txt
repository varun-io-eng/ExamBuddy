# ─────────────────────────────────────────────────────────────
# Exam Buddy Pro — requirements.txt
# Optimised for fast Streamlit Cloud cold-start
#
# HEAVY OPTIONAL DEPS (torch, chromadb, sentence-transformers)
# are intentionally excluded — the app falls back automatically:
#   DKT          → numpy-based BKT  (deep_knowledge_tracker.py)
#   ChromaDB RAG → TF-IDF retrieval (rag_retriever.py)
#   PyTorch ML   → numpy neural net (ml_difficulty_predictor.py)
#
# To enable full ML locally:
#   pip install torch chromadb sentence-transformers
# ─────────────────────────────────────────────────────────────

# ── Core UI ─────────────────────────────────────────────────
streamlit>=1.32.0

# ── LLM API ─────────────────────────────────────────────────
groq>=0.9.0

# ── Data & ML (lightweight) ──────────────────────────────────
numpy>=1.26.0
pandas>=2.1.0
scikit-learn>=1.4.0

# ── Visualisation ────────────────────────────────────────────
plotly>=5.20.0

# ── PDF generation (Study Coach export) ──────────────────────
reportlab>=4.1.0

# ── PDF reading (Upload & Take Exam tab) ─────────────────────
PyPDF2>=3.0.0